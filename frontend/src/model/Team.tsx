type Team = {
  id: string;
  name: string;
  description: string;
  organisation_name: string;
  organisation_description: string;
  created_at?: number;
  update_at?: number;
};

export default Team;
